spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/soundstore_dev}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

management:
  endpoint:
    health:
      show-details: always
